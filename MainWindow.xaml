<Window x:Class="NewYearGarlandGuide.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Новогодняя Гирлянда - Инструкция" Height="747" Width="748"
        WindowState="Maximized" 
        WindowStyle="None"
        Background="#0A0A14"
        Foreground="#E0E0FF"
        FontFamily="Segoe UI"
        ResizeMode="CanMinimize">

    <Grid Margin="0,0,0,-16">
        <Grid.RowDefinitions>
            <RowDefinition Height="616*"/>
            <RowDefinition Height="111*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>

        <Rectangle Grid.RowSpan="2" Margin="0,0,0,10">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#0A0A14" Offset="0"/>
                    <GradientStop Color="#121225" Offset="0.5"/>
                    <GradientStop Color="#0A0A14" Offset="1"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center" 
                    Width="500" Margin="0,60,0,0" Panel.ZIndex="1" Height="580" Grid.RowSpan="2">

            <Border Background="#1A1A2E" CornerRadius="15" Padding="30,20" 
                    Margin="0,0,0,20" BorderBrush="#3A3AFF" BorderThickness="2">
                <StackPanel>
                    <TextBlock Text="НОВОГОДНЯЯ ГИРЛЯНДА" FontSize="28" FontWeight="Bold" Foreground="#6CE5FF" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                    <TextBlock Text="Полное руководство по установке" FontSize="18" Foreground="#A0A0FF" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <TextBlock Text="ВЫБЕРИТЕ ТИП ГИРЛЯНДЫ:" Foreground="#FFD700" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

            <ComboBox x:Name="GarlandTypeSelector" SelectedIndex="0" Height="45" Margin="0,0,0,20" 
                      Foreground="White" FontSize="16" BorderThickness="0">
                <ComboBox.Resources>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="#1A1A2E"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="gd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="gd" Property="Background" Value="#3A3AFF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ComboBox.Resources>

                <ComboBox.Template>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Grid.Column="2" ClickMode="Press" Focusable="false" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" Background="#1A1A2E" BorderBrush="#3A3AFF" BorderThickness="2" CornerRadius="8">
                                            <Path x:Name="Arrow" Fill="#6CE5FF" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Data="M0,0 L4,4 L8,0 Z"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#252545"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" Margin="15,3,23,3" VerticalAlignment="Center" HorizontalAlignment="Left" />
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid x:Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="#1A1A2E" BorderBrush="#3A3AFF" BorderThickness="2" CornerRadius="5"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </ComboBox.Template>

                <ComboBoxItem Content="🎄 Обычная гирлянда-нить"/>
                <ComboBoxItem Content="🕸️ Гирлянда-сетка"/>
                <ComboBoxItem Content="🕯️ Гирлянда со свечами"/>
            </ComboBox>

            <Button x:Name="StartButton" Content="🚀 НАЧАТЬ ИНСТРУКЦИЮ" Height="70" FontSize="20" FontWeight="Bold" Background="#006644" Foreground="White" BorderBrush="#00FF88" BorderThickness="2" Click="Start_Click" Margin="0,0,0,20" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button x:Name="ExitButton" Content="🚪 ВЫЙТИ ИЗ ПРИЛОЖЕНИЯ" Height="50" FontSize="18" FontWeight="SemiBold" Background="#3A3A6A" Foreground="White" BorderBrush="#6A6AFF" BorderThickness="1" Click="Exit_Click" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Border Background="#1A1A2E" CornerRadius="10" Padding="20,15" BorderBrush="#FFD700" BorderThickness="1" Margin="0,20,0,0">
                <StackPanel>
                    <TextBlock Text="📋 О ПРИЛОЖЕНИИ:" FontSize="16" FontWeight="Bold" Foreground="#FFD700" Margin="0,0,0,8"/>
                    <TextBlock Text="• Поддержка 3-х видов гирлянд" FontSize="14" Foreground="#D0D0FF"/>
                    <TextBlock Text="• Наглядные пошаговые фото" FontSize="14" Foreground="#D0D0FF"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <Border VerticalAlignment="Center" HorizontalAlignment="Center" Background="#121225" Padding="15,10" CornerRadius="10" BorderBrush="#3A3AFF" BorderThickness="1" Panel.ZIndex="2" Height="42" Width="304" Grid.Row="1">
            <TextBlock Text="✨ С наступающим Новым Годом! ✨" FontSize="15" Foreground="#A0A0FF" FontStyle="Italic" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>