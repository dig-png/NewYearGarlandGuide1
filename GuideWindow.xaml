<Window x:Class="NewYearGarlandGuide.GuideWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Title="Инструкция по установке гирлянды" Height="650" Width="750"
 WindowStartupLocation="CenterScreen"
 Background="#0A0A14"
 Foreground="#E0E0FF"
 FontFamily="Segoe UI"
 MinHeight="650" MinWidth="750"
 MaxHeight="800" MaxWidth="900">

    <Grid>

        <Rectangle>
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#0A0A14" Offset="0"/>
                    <GradientStop Color="#121225" Offset="0.5"/>
                    <GradientStop Color="#0A0A14" Offset="1"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="300"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>


            <TextBlock Grid.Row="0" Text="🎄 ИНСТРУКЦИЯ ПО УСТАНОВКЕ ГИРЛЯНДЫ 🎄"
                 FontSize="24" FontWeight="Bold"
                 Foreground="#6CE5FF" 
                 TextAlignment="Center"
                 Margin="0,0,0,15"/>


            <Border Grid.Row="1" BorderBrush="#3A3AFF" BorderThickness="2" 
              Background="#1A1A2E" CornerRadius="12" Margin="225,10,195,20">
                <Grid>
                    <Border CornerRadius="10" Margin="2">
                        <Image x:Name="StepImage" Stretch="Uniform" 
                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Row="2" BorderBrush="#FFD700" BorderThickness="1" 
              Background="#1A1A2E" CornerRadius="10" Padding="20" Margin="0,0,0,15">
                <TextBlock x:Name="StepText" FontSize="16" TextWrapping="Wrap"
                   LineHeight="24" TextAlignment="Left"
                   Foreground="#D0D0FF" TextTrimming="None"/>
            </Border>

            <Grid Grid.Row="3" Margin="0,0,0,15">
                <Border Background="#121225" Height="14" CornerRadius="7">
                    <Border x:Name="ProgressFill" Background="#00FF88" 
                  Width="0" HorizontalAlignment="Left" 
                  CornerRadius="7"/>
                </Border>

            </Grid>

            <Border Grid.Row="4" HorizontalAlignment="Center" 
              Background="#1A1A2E" CornerRadius="20" Padding="15,8"
              BorderBrush="#3A3AFF" BorderThickness="1" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="ШАГ" FontWeight="Bold" Foreground="#A0A0FF" Margin="0,0,8,0" FontSize="16"/>
                    <TextBlock x:Name="CurrentStepNumber" Text="1" FontWeight="Bold" 
                     FontSize="18" Foreground="#6CE5FF" Margin="0,0,5,0"/>
                    <TextBlock Text="из" Foreground="#8080FF" Margin="0,0,5,0" FontSize="16"/>
                    <TextBlock Text="5" Foreground="#8080FF" FontSize="16"/>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="5" Orientation="Horizontal"
                  HorizontalAlignment="Center"/>
            <Button x:Name="NextButton" Content="ДАЛЕЕ ▶" 
                FontSize="14" FontWeight="SemiBold"
                Background="#006644" Foreground="White"
                BorderBrush="#00FF88" BorderThickness="1"
                Click="Next_Click"
                Cursor="Hand" Margin="560,21,0,0" Grid.Row="4">
                <Button.Template>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}"
                      BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}"
                      CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" 
                                  VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="BackButton" Content="◀ НАЗАД" 
                FontSize="14" FontWeight="SemiBold"
                Background="#3A3A6A" Foreground="White"
                BorderBrush="#6A6AFF" BorderThickness="1"
                Click="Back_Click"
                Cursor="Hand" Margin="28,41,532,0" Grid.RowSpan="2" Grid.Row="4">
                <Button.Template>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}"
                      BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}"
                      CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" 
                                  VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="BackButton_Копировать" Content="◀ НАЗАД" 
                FontSize="14" FontWeight="SemiBold"
                Background="#3A3A6A" Foreground="White"
                BorderBrush="#6A6AFF" BorderThickness="1"
                Click="Back_Click"
                Cursor="Hand" Margin="28,21,532,0" Grid.Row="4">
                <Button.Template>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" 
                                VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Grid>
</Window>